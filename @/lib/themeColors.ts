export const COLOR_MAP = {
  "violet": {
    primary: "#7A288A",
    secondary: "#9B59B6",
    base: "#f7f7f7"
  },
  // Add other color themes as needed
};

export function getMeshStyle(themeColor: string) {
  // Implement logic to generate mesh style based on themeColor
}

export function getScrollbarColor(themeColor: string) {
  // Implement logic to generate scrollbar color based on themeColor
}